<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <link rel="stylesheet" href="css/style.css">
  <meta charset="utf-8">
  <meta name="description" content="To Do List API">
  <meta name="keywords" content="api, todo, project">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>To-Do List</title>
</head>
<body>
  <h1>To-Do List</h1>
  <ul class="todolist">
  <% for(let i=0; i < info.length; i++) {%>
      <li class="todotask">
          <% if(info[i].completed === true) {%>
              <span class='complete'><%= info[i].taskName %></span>
              <span class='fa fa-times'></span>
          <% }else{ %>
              <span><%= info[i].taskName %></span>
              <span class='fa fa-check'></span>
          <% } %>
          <span id='trash' class='fa fa-trash'></span>
      </li>
  <% } %>
  </ul>

  <h2>Add A Task:</h2>

  <form action="/addTask" method="POST">
      <input type="text" placeholder="Task" name="taskName">
      <input type="submit">
  </form>

</body>
<script src='js/main.js'></script>
</html>
